# grok-cli-termux-native
Run xAI's Grok Build CLI (grok) natively on Termux (aarch64) — no proot, no root, no reboot. Static musl binary + a single 16-byte DNS byte-patch (/etc/resolv.conf → /sdcard/.grokdns). One-command installer.
